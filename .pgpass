# Create .pgpass file
$pgpassContent = "localhost:5433:memecoin_discovery:memecoin_user:Bhaal1313!#!#"
$pgpassPath = "$env:USERPROFILE\.pgpass"

# Write the file
Set-Content -Path $pgpassPath -Value $pgpassContent -NoNewline

# Set restrictive permissions (Windows doesn't use this, but good practice)
# On Linux/Mac you'd do: chmod 600 ~/.pgpass